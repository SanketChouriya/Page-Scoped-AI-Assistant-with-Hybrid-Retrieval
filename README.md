# Page-Scoped-AI-Assistant-with-Hybrid-Retrieval
A "page-scoped AI assistant" that answers user questions using only the content of the currently viewed webpage.  The system combines PostgreSQL full-text search with semantic retrieval to maximize accuracy while minimizing latency.
